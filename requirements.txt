# OpenAI Realtime API 兼容服务器依赖
#
# 推荐使用 uv 管理依赖（依赖定义在 pyproject.toml 中）：
#   uv sync                    # 安装核心依赖
#
# 如果不使用 uv，也可以用 pip：
#   pip install -r requirements.txt

# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# 音频处理
numpy>=1.24.0
scipy>=1.11.0

# 环境变量加载
python-dotenv>=1.0.0

# HTTP 异步客户端
aiohttp>=3.9.0

# ==================== STT 服务依赖 ====================
# Deepgram (推荐，需要 API Key)
deepgram-sdk>=5.0.0

# OpenAI Whisper (需要 API Key)
openai>=1.0.0

# 本地 Whisper (可选，需要 GPU 效果更好)
# openai-whisper>=20231117

# ==================== TTS 服务依赖 ====================
# Edge TTS (免费，微软 Edge 浏览器 TTS)
edge-tts>=6.1.0

# PyTorch - Silero VAD 模型运行时（通过 torch.hub 自动下载 VAD 模型）
torch>=1.12.0
# torchaudio - 某些音频处理路径/依赖会显式 import torchaudio
torchaudio>=0.12.0
